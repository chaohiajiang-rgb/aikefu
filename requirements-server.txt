# 仅后端服务所需依赖（不含 desktop_client.py 的 customtkinter / requests）
# 用于内网 Windows 服务器一键部署，避免在无界面服务器上安装 GUI 依赖失败
# 版本锁定 = Mac 上验证通过的组合；离线 wheels 见 deploy/wheels/（py3.12/3.13）
# 注意：不用 uvicorn[standard]——其 extras（uvloop/httptools/watchfiles）在
# Windows 上部分不可装，离线 pip 会解析失败；标准版 uvicorn 功能完全够用。
fastapi==0.141.1
uvicorn==0.53.0
openai==3.14.1
python-dotenv==1.1.0
